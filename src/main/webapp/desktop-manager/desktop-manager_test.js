'use strict';

describe('strudelWeb.desktop-manager module', function() {

  beforeEach(module('strudelWeb.desktop-manager'));

  describe('desktop-manager controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var desktopManagerCtrl = $controller('DesktopManagerCtrl');
      expect(desktopManagerCtrl).toBeDefined();
    }));

  });
});