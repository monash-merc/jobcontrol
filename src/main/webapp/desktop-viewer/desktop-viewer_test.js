'use strict';

describe('strudelWeb.desktop-viewer module', function() {

  beforeEach(module('strudelWeb.desktop-viewer'));

  describe('desktop-viewer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var desktopViewerCtrl = $controller('DesktopViewerCtrl');
      expect(desktopViewerCtrl).toBeDefined();
    }));

  });
});