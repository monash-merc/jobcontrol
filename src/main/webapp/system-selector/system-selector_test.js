'use strict';

describe('strudelWeb.system-selector module', function() {

  beforeEach(module('strudelWeb.system-selector'));

  describe('system-selector controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var systemSelectorCtrl = $controller('SystemSelectorCtrl');
      expect(systemSelectorCtrl).toBeDefined();
    }));

  });
});